# familiar

Desktop/terminal/agent familiar engine. Themes install separately: place a
theme package's export under `~/.config/familiar/themes/<id>` (or point
`FAMILIAR_THEMES_DIR` at a root that contains it); this repo ships no art.

- Run tests: `npm test`
- Pre-split history: https://github.com/khughitt/familiar-archive
