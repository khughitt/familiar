---
id: fam-6e8321
title: Consume relay identity and reconcile familiar presentation
status: todo
priority: 1
size: l
complexity: high
process: planned
created: 2026-09-17T00:50:54Z
updated: 2026-09-17T00:50:54Z
depends: [relay-c19c0a]
tags: []
source: ops-998bbb
agent: codex
---

Approved source: ops docs/specs/2026-09-16-relay-design.md (reviewed after d7e2c33); execution brief: ops docs/plans/2026-09-16-relay-bootstrap.md (approved after bc53c51 with review corrections).

Next: write a focused implementation plan against the approved spec for user review; add a spec addendum only if inspection uncovers an unresolved contract.

Change familiar `package.json` and `package-lock.json` to the public tag/commit from
relay-c19c0a. Add the observer/reconcile path to `bin/familiar.js` and refactor
`src/bus/transaction.js` into presentation-only projection. Update the direct
process imports in `integrations/niri/window.js`, statusline paths, installer/setup
paths and OpenCode callers; search all imports again before deleting extracted
modules. `src/config.js` and `src/theme/receipt.js` also use the existing store
helper: preserve their atomic writes with an intentional local helper or a named
public export, never an old-path forwarding module.

Under familiar's presentation lock, read the newest relay snapshot, preserve prior
intent for terminal cleanup, resolve visuals and write the existing intent format.
Project keys from Agent.sessionId; reject collisions, and let broken themes omit
visuals without evicting registry agents. Keep visual state decay, slots, pins,
sprites, terminal behavior and cosmetic failure policy here. Add relay-state parity
coverage against familiar-theme's six-state vocabulary.

Extend `test/{transaction,resolve,state,terminal-target,emit,statusline,
bin-familiar,bin-familiar-niri,bin-familiar-noctalia}.test.js` and existing OpenCode
concurrency/terminal-cleanup tests. Test overlapping observers, generation reset,
missed observer recovery, SessionEnd/removal, no-prune reconciliation and a bad theme.
Update `docs/install.md`: scheduled reap becomes prune plus reconcile even when
nothing is pruned; startup/explicit refresh also reconcile. Timer recovery updates
files, without terminal emission using the timer's environment or replaying bells.
Live changes belong to A; migration code alone must not install hooks.

Prerequisites: relay-c19c0a. Release goal: relay-1231bf. Bootstrap ops-998bbb closure does not mean this deliverable has shipped.
