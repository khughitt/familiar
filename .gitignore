node_modules/

# SDD workspace (ledgers, review packages, absolute local paths). Ignored
# globally on this machine, but listed here too so a fresh clone, another
# machine, or a contributor's `git add -A` can't re-stage it.
.worktrees/
.superpowers/

# Mutation fixture a test must build INSIDE the repo, because the specifier
# under test only resolves from a path under the workspace root: a bare
# `familiar-theme` import. Removed in a finally; this pattern covers the
# hard-crash case.
/slots-guard-*
